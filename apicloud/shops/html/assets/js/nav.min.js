/*
var showRel = 0
    , resetScrollEle = function() {
    var a = $("#cate_lev2_div_" + showRel + ">li").find("a").eq(0).width() + "px";
    $(".cate_icon_img").css({
        width: a,
        height: a
    })
};

window.addEventListener("resize", function() {
    resetScrollEle()
});
window.addEventListener("orientationchange", function() {
    resetScrollEle()
});
var lv2S, nav = $(".class_list_nav_box ul>a"), cSrc = function(a) {
    a.each(function(a, b) {
        var c = $(b).attr("data-src")
            , d = $(b).attr("src");
        c !== d && $(b).attr("src", c)
    })
};
lv1S = new iScroll("class_list_nav_box",{
    hScrollbar: !1,
    vScrollbar: !1,
    hScroll: !1,
    vScroll: !0
});
lv2S = new iScroll("class_box_inner",{
    hScrollbar: !1,
    vScrollbar: !1,
    hScroll: !1,
    vScroll: !0,
    checkDOMChanges: !0
});
*/
/*var myScrollleft,myScrollright;
function loaded () {
    myScrollleft = new IScroll('#wrapper', { mouseWheel: true, click: true });
    myScrollright = new IScroll('#class_box_inner', { mouseWheel: true, click: true });
}


document.addEventListener('touchmove', function (e) { e.preventDefault(); }, isPassive() ? {
        capture: false,
        passive: false
    } : false);
function isPassive() {
        var supportsPassiveOption = false;
        try {
            addEventListener("test", null, Object.defineProperty({}, 'passive', {
                get: function () {
                    supportsPassiveOption = true;
                }
            }));
        } catch(e) {}
    return supportsPassiveOption;
}
$(function () {
    setTimeout(function () {
        loaded();
    },1000);
});*/
